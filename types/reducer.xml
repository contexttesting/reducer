<types>
  <type name="Config" desc="Options for the reducer.">
    <prop boolean name="onlyFocused" default="false">
      Run only focused tests.
    </prop>
    <prop type="(test: Test) => Promise.<*>" name="runTest">
      The function used to run a test. It will receive `name`, `context`, `fn`, and `timeout` properties.
    </prop>
    <prop type="(testSuite: TestSuite) => Promise.<TestSuiteLite>" name="runTestSuite">
      The function used to run a test suite. It will receive `name`, `tests` and `onlyFocused` properties.
    </prop>
  </type>
  <type type="Object.<string, Test|Object.<string, Test|Object.<string, Test>>>" name="TestSuiteLite" desc="An recursive tree returned by the reducer containing either nested test suites or tests updated with the outcome of the runTest method (not pure since the test methods passed are mutated)." />
  <type name="TestSuite" desc="The structure which will be passed to the `runTestSuite` method.">
    <prop string name="name">The name of the test suite.</prop>
    <prop type="Test[]" name="tests">Tests.</prop>
    <prop boolean name="onlyFocused">Run only focused tests.</prop>
  </type>
</types>